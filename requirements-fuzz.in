# Deps for the Atheris fuzz harness (fuzz/). Linux-only: atheris ships manylinux
# wheels and is imported only by the fuzz job, which runs on ubuntu + Python 3.12.
# Keep these floors in sync with pyproject.toml [project.dependencies].
langchain>=1.3.9,<2
langchain-core>=1.3.3,<2
langchain-ollama>=1.0,<2
langchain-mcp-adapters>=0.3,<1
mcp>=1.24,<2
pyyaml>=6.0
atheris>=3.1
